services:
  caddy:
    volumes:
      - /opt/compose/apps/agent-memory/Caddyfile.shared-edge:/etc/caddy/sites/agent-memory.caddy:ro
    networks:
      default:
      opspresso-edge:

networks:
  opspresso-edge:
    external: true
