apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-memory.fullname" . }}
  labels:
    {{- include "agent-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "agent-memory.appSelectorLabels" . | nindent 4 }}
